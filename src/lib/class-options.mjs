// Visibility can change without deleting older class data or Drive folders.
export const visibleClasses = [12, 11, 10, 9];
export const boardForClass = grade => ['11', '12'].includes(String(grade)) ? 'ISC' : 'ICSE';
